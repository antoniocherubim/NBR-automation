[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "nbr12721"
version = "0.0.0"
description = "Automação determinística da ABNT NBR 12721:2006 (scaffold inicial)"
readme = "ROADMAP.md"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]
